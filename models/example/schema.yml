version: 2

models:
  - name: my_first_dbt_model
    description: Starter model retained for a one-time dbt connection test.
    columns:
      - name: id
        description: Example identifier; the starter data intentionally contains one null.
        tests:
          - unique
          - not_null
